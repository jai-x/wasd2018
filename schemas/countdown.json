{
	"$schema": "https://json-schema.org/draft-04/schema#",

	"type": "object",

	"additionalProperties": false,

	"properties": {
		"running": {
			"type": "boolean",
			"default": false
		},
		"time": {
			"type": "string",
			"default": ""
		}
	},

	"required": ["time", "running"]
}
